import { ForceRefreshDirective } from './force-refresh.directive';

describe('AppRerenderDirective', () => {
  it('should create an instance', () => {
    const directive = new ForceRefreshDirective();
    expect(directive).toBeTruthy();
  });
});
